<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/style.css">
    <title>Weather</title>
  </head>
  <body>
    <div class="container">
      <header>
        <div class="compass-icon"><img src="/icons/compass.svg" alt="" /></div>
      </header>
      <div class="weather-title">
        <h1>Weather</h1>
        <p>Veja a temperatura atual de qualquer cidade</p>
      </div>
      <form action="/" method="POST" id="post-form">
        <input
          type="text"
          name="city"
          class="ghost-input"
          placeholder="Digite uma cidade"
        />
      </form>
      
      <div class="result">
        <% if(city !== null) {%>
          <h3 class="city"><%= city %></h3>
          <% } %>
        <% if(temp !== null) {%>
        <h1 class="temp"><%= temp %>°</h1>
        <% } %>

        <div class="icon-des">
          <% if(icon !== null) {%>
          <img
            class="icon"
            src="http://openweathermap.org/img/w/<%= icon %>.png"
            alt=""
          />
          <% } %> <% if(des !== null) {%>
          <h3 class="des"><%= des %></h3>
          <% } %>
        </div>
        <% if(hum !== null) {%>
        <h3>Umidade: <%= hum %>%</h3>
        <% } %>
        <% if(feel !== null) {%>
          <h3>Sensação térmica: <%= feel %>°</h3>
          <% } %>
      </div>
      </div>
    </div>
  </body>

</html>
